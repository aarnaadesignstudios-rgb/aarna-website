# /public/images

Local, optimised image assets live here.

**Phase 1** uses remote Unsplash placeholders (whitelisted in `next.config.ts`).

TODO: replace every remote `Media` src across `constants/content.ts` and the
sections with final assets committed to this folder, then remove the Unsplash
`remotePatterns` entry from `next.config.ts`.
